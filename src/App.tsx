import { Button } from "@/components/Elements";

import "./App.css";
function App() {
	return (
		<div>
			<Button href="#">see the work</Button>
			<Button variant="text" type="button" role="button">
				<span>Menu</span>
			</Button>
		</div>
	);
}

export default App;
